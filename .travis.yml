language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bayse' ]; then cd .. && eval "mv $currentfolder generator-bayse" && cd generator-bayse; fi

